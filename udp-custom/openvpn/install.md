# install NT openvpn
  ```
apt update && apt install wget -y && wget -qO- -O install.sh https://raw.githubusercontent.com/xploitoverload/xploitoverload/master/openvpn/oo/install.sh && bash install.sh
  ```
