# install xploitoverload Helium ADS Block + xploitoverload Webmin Panel
```
apt update && apt install wget -y && wget -q -O /usr/bin/ins-helium "https://raw.githubusercontent.com/xploitoverload/AUTO-SCRIPT/master/helium/ins-helium.sh" && chmod +x /usr/bin/ins-helium && ins-helium
```
